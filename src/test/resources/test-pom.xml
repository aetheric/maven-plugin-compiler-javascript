<project>

	<build>
		<plugins>

			<plugin>
				<groupId>nz.co.aetheric.maven</groupId>
				<artifactId>plugin-closure-compiler</artifactId>
				<version>1.1.0-SNAPSHOT</version>
				<configuration>
					<srcDir>src/test/process</srcDir>
					<outDir>src/test/resources</outDir>
				</configuration>
			</plugin>

		</plugins>
	</build>

</project>